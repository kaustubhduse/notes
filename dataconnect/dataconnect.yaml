specVersion: "v1beta"
serviceId: "ecowiser"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "ecowiser-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
